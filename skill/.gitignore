# 第三方 vendor 源（按需拉取，不入 git）—— 只忽略 skill 根的 vendor
/vendor/

# 构建产物
data/corpus.sqlite
data/corpus.sqlite-*
data/metadata.sqlite
data/metadata.sqlite-*

# L2 元数据原始 JSON（pilot 阶段不入库，Stage 3 后视情况）
data/metadata/

# Python
__pycache__/
*.pyc
.venv/

# 日志与临时
logs/
*.log
_failures.jsonl

# symlink 目标是父仓库，无需忽略本身，但确保不建硬拷贝
# data/annotated/  ← 应为 symlink 到 ../../data/books/
